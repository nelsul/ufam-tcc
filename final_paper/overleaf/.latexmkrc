# Latexmk configuration file
# Output directory configuration

# Set output directory
$out_dir = 'Main/out';

# Ensure PDF is generated
$pdf_mode = 1;

# Clean up auxiliary files - only keep PDF in output directory
$clean_ext = 'synctex.gz aux log out fls fdb_latexmk bbl blg toc lof lot';
